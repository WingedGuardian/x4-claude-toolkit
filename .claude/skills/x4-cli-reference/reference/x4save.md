<!-- GENERATED by tools/x4validate/scripts/gen-cli-reference.py from each CLI's own argparse help. Do not edit: regenerate. -->

# `x4save`

```text
usage: x4save [-h] [--version] {info,check} ...

Read an X4 savegame: what is baked into it, and what it references that the live tree no longer
defines.

positional arguments:
  {info,check}
    info        header: build, playtime, and the SAVE-BAKED extensions
    check       macro references the live tree no longer defines

options:
  -h, --help    show this help message and exit
  --version     show program's version number and exit
```

## `x4save info`

```text
usage: x4save info [-h] [save]

positional arguments:
  save        path or save name (default: newest)

options:
  -h, --help  show this help message and exit
```

## `x4save check`

```text
usage: x4save check [-h] [--limit LIMIT] [save]

positional arguments:
  save           path or save name (default: newest)

options:
  -h, --help     show this help message and exit
  --limit LIMIT  names to print (default 40)
```
