<!-- GENERATED by tools/x4validate/scripts/gen-cli-reference.py from each CLI's own argparse help. Do not edit: regenerate. -->

# `x4modlist`

```text
usage: x4modlist [-h] [--version] [--registry REGISTRY]
                 {ingest,refresh,dashboard,needs-review,resolve,source,verify,tracked,ignore,mark,changed,snapshot} ...

X4 mod-registry triage tool (API-first).

positional arguments:
  {ingest,refresh,dashboard,needs-review,resolve,source,verify,tracked,ignore,mark,changed,snapshot}
    ingest              scan installed extension folders (PRIMARY) into the registry, cross-
                        checked against the profile content.xml
    refresh             refresh upstream metadata via Nexus/Steam API
    dashboard           regenerate WORKLIST.md from the registry
    needs-review        list entries needing a spot-check decision
    resolve             pin a mod's Nexus identity permanently (+fetch)
    source              record a non-Nexus origin (stops it being searched)
    verify              list identities that are guesses, with the denominator
    tracked             what the ACCOUNT tracks on Nexus vs what is INSTALLED
    ignore              mark a junk/personal mod out of the active worklist
    mark                mark a mod as custom-edited (for /x4-update-mod)
    changed             localise a moved content fingerprint: WHICH mod changed, and how
    snapshot            record the current mod vector as a cheap baseline (~0.1s), for the gap
                        between expensive rebuilds

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
  --registry REGISTRY   path to modlist.yaml (default: dev\_registry\modlist.yaml)
```

## `x4modlist ingest`

```text
usage: x4modlist ingest [-h] [--content CONTENT] [--dirs DIRS] [--installed-only] [--build BUILD]
                        [--all]

options:
  -h, --help         show this help message and exit
  --content CONTENT  path to profile content.xml (secondary cross-check)
  --dirs DIRS        comma-separated extension dirs to scan (default: game-root extensions\,
                     profile extensions\, Steam Workshop)
  --installed-only   skip the profile content.xml cross-check pass entirely
  --build BUILD      current game build id. Default: UNKNOWN -- the registry records None and the
                     dashboard prints '?'. It used to default to a build id pinned in the source,
                     which wrote a GUESS into every user's registry in the slot a measurement
                     belongs in; `x4_acf_buildid` in .claude/hooks/_x4-env.sh reads the installed
                     build from the Steam manifest (F123)
  --all              content.xml cross-check: include disabled extensions too
```

## `x4modlist refresh`

```text
usage: x4modlist refresh [-h] [--ids IDS] [--seeded] [--limit LIMIT] [--force] [--no-resolve]

options:
  -h, --help     show this help message and exit
  --ids IDS      comma-separated content ids to refresh (default: all enabled)
  --seeded       only mods that already have a nexus_id
  --limit LIMIT  cap how many mods to process (API-call safety)
  --force        ignore the once-per-day TTL
  --no-resolve   skip A3 identity resolution
```

## `x4modlist dashboard`

```text
usage: x4modlist dashboard [-h]

options:
  -h, --help  show this help message and exit
```

## `x4modlist needs-review`

```text
usage: x4modlist needs-review [-h]

options:
  -h, --help  show this help message and exit
```

## `x4modlist resolve`

```text
usage: x4modlist resolve [-h] [--file FILE] id nexus_id

positional arguments:
  id           content.xml extension id
  nexus_id     the correct Nexus mod id, or 'none' if it has no page

options:
  -h, --help   show this help message and exit
  --file FILE  file id, when this mod ships as a FILE on that page (an add-on); update-detection
               then tracks the FILE's version, not the page's
```

## `x4modlist source`

```text
usage: x4modlist source [-h] id source

positional arguments:
  id          content.xml extension id
  source      steam:<publishedfileid> | local | bundled:<mod-id> | <url>

options:
  -h, --help  show this help message and exit
```

## `x4modlist verify`

```text
usage: x4modlist verify [-h] [--rescore]

options:
  -h, --help  show this help message and exit
  --rescore   offline: promote a guess to `exact` only where the installed manifest name is
              IDENTICAL to the stored upstream title (no API calls, no near-matches)
```

## `x4modlist tracked`

```text
usage: x4modlist tracked [-h] [--domain DOMAIN] [--limit LIMIT]

options:
  -h, --help       show this help message and exit
  --domain DOMAIN  Nexus game domain (the endpoint is account-wide across all games) (default:
                   x4foundations)
  --limit LIMIT    rows to show per list (default: 20)
```

## `x4modlist ignore`

```text
usage: x4modlist ignore [-h] [--reason REASON] id

positional arguments:
  id               content.xml extension id

options:
  -h, --help       show this help message and exit
  --reason REASON  note why
```

## `x4modlist mark`

```text
usage: x4modlist mark [-h] [--custom] [--notes NOTES] id

positional arguments:
  id             content.xml extension id

options:
  -h, --help     show this help message and exit
  --custom       (implied) set custom_edited
  --notes NOTES  what you edited
```

## `x4modlist changed`

```text
usage: x4modlist changed [-h] [--since SINCE] [--files] [--usn]

options:
  -h, --help     show this help message and exit
  --since SINCE  baseline: store (default) | xref | latest | <path to a snapshot json>
  --files        name the exact files that changed inside each mod
  --usn          advisory NTFS change-journal corroboration (needs an elevated shell; immune to
                 back-dating and to NTFS tunneling)
```

## `x4modlist snapshot`

```text
usage: x4modlist snapshot [-h] [--label LABEL]

options:
  -h, --help     show this help message and exit
  --label LABEL  short name to append to the file
```
